
import React from 'react';
import ReactDOM from 'react-dom/client';
import AppWallSystem from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppWallSystem />
  </React.StrictMode>
);
